<Page x:Class="WarehouseManager_Toshmatov.Views.Main"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:elements="clr-namespace:WarehouseManager_Toshmatov.Views.Items"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Main">

    <Page.Resources>
        <Style x:Key="Update" TargetType="Button">
            <Setter Property="Background" Value="#FF4FC318"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#FF4FC318"/>
            <Setter Property="BorderBrush" Value="#FF4FC318"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10" HorizontalAlignment="Center">
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"   Width="250" Height="30" 
                     VerticalContentAlignment="Center"  Margin="0,0,10,0"   ToolTip="Поиск по названию продукта"/>

            <ComboBox ItemsSource="{Binding Suppliers}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedSupplier}"
                      Width="150" Height="30" Margin="0,0,10,0" ToolTip="Фильтр по поставщику"/>
            <Button Command="{Binding OnResetFilters}" Content="Сбросить" Width="80" Height="30"/>
        </StackPanel>
        
        <ListView Grid.Row="2"  ItemsSource="{Binding Orders}" 
                  HorizontalContentAlignment="Stretch"  Margin="10,0,10,10">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <elements:OrderItem DataContext="{Binding}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Button Grid.Row="3"  Command="{Binding OnAddOrder}"  Content="Добавить"  Height="25" Width="70"  
                VerticalAlignment="Bottom"  HorizontalAlignment="Right" Margin="0,0,10,10">
            <Button.Effect>
                <DropShadowEffect Opacity="0.5" BlurRadius="3" ShadowDepth="3"/>
            </Button.Effect>
        </Button>
    </Grid>
</Page>